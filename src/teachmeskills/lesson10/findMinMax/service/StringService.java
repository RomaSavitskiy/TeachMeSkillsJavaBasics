package teachmeskills.lesson10.findMinMax.service;

public class StringService {
    public void findShortWord(String string) {
        char[] arrayChar = string.toCharArray();
        int i = 0;

        while (((arrayChar[i] > 96) & (arrayChar[i] < 122)) | (i <= arrayChar.length)) {

        }
    }
}
